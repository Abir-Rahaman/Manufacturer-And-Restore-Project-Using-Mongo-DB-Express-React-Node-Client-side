import React from 'react';

const Loading = () => {
    return (
        <div>
            <div className='flex h-screen justify-center items-center' >
            <button className="btn loading bg-primary rounded-full btn-primary"> Please Wait... </button>
        </div>
        </div>
    );
};

export default Loading;